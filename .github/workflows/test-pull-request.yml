name: Test Pull Request

on:
  pull_request:
    branches:
    - main

jobs:
  test-pull-request:
    name: Test Pull Request
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Build
      run: |
        ./scripts/build.sh
